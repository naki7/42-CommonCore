# Tells flake8 to skip the virtual environment and Python cache folders
# when running `flake8 .`. Without this, flake8 would lint every file
# inside venv/ and crash on style issues in third-party packages.

[flake8]
exclude = matrix,venv,.venv,__pycache__,.mypy_cache